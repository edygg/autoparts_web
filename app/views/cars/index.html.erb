<div class="container">
  <div class="col-md-8 col-md-offset-2">
    <h1>Lista de autos disponibles</h1>

    <table class="table table-hover">
      <thead>
        <tr>
          <th>Marca</th>
          <th>Modelo</th>
          <th>Año</th>
          <th></th>
          <!-- <th></th> -->
        </tr>
      </thead>

      <tbody>
        <% @cars.each do |car| %>
          <tr>
            <td><%= car.brand %></td>
            <td><%= car.model %></td>
            <td><%= car.year %></td>
            <td><%= link_to 'Editar', edit_car_path(car), class: "btn btn-sm btn-info" %></td>
            <!-- <td><%= link_to 'Destroy', car, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to 'Agregar un auto', new_car_path, class: "btn btn-success" %>
  </div>
</div>