<%= form_for(@car, class: "form-horizontal") do |f| %>
  <% if @car.errors.any? %>
    <div id="error_explanation" class="alert alert-danger"> 
      <strong><%= pluralize(@car.errors.count, "error") %> prohibited this car from being saved:</strong>

      <ul>
      <% @car.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :brand, "Marca", class: "control-label col-sm-4" %>
    <div class="col-sm-8">
      <%= f.text_field :brand, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :model, "Modelo", class: "control-label col-sm-4" %>
    <div class="col-sm-8">
      <%= f.text_field :model, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :year, "Año", class: "control-label col-sm-4" %>
    <div class="col-sm-8">
      <%= f.number_field :year, class: "form-control" %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-success" %>
  </div>
<% end %>
